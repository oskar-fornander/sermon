
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sermon"
version = "0.1.0"
description = "Predikoarkiv – CLI"
dependencies = [
    "typer[all]",
    "pyyaml",
    "send2trash",
]

[project.scripts]
sermon = "app.cli:app"
