
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sermon"
version = "0.1.0"
description = "Predikoarkiv – CLI"
dependencies = [
    "typer>=0.9",
    "PyYAML>=6.0",
    "send2trash>=1.8"
]

[project.scripts]
sermon = "app.cli:run"
