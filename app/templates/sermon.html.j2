<!doctype html>
<!-- sermon.html -->
<html lang="sv">
    <head>
        <meta charset="utf-8">
        <title>Predikoregister – {{ user }}</title>
        <style>
            /* Style for html page */
            {% include "sermon.css" %}
        </style>
    </head>
    <body>
        <header>

        </header>
        <main>
            {% set ns = namespace(index=0) %}

            <h1>Predikoregister – {{ user }}</h1>

            <div id="search">
                <input id="searchInput" type="search" placeholder="Sök ..." />
                <div id="searchResult"></div>
            </div>

            <table id="sermon" data-sort-direction="ASC" data-sort-column="0">
                <thead>
                    <tr class="heading">
                        <th class="code desc" onclick="sortTable(0)">Kod</th>
                        <th class="title" onclick="sortTable(1)">Titel</th>
                        <th class="date hidden" onclick="sortTable(2)">Datum</th>
                        <th class="all-dates" onclick="sortTable(2)">Datum</th>
                        <th class="bible-references" onclick="sortTable()">Bibelreferenser</th>
                    </tr>
                </thead>
                <tbody>
                    {% for s in sermons %}
                    <tr data-index="{{ ns.index }}">
                        <td class="code" onclick="toggleData('{{ ns.index }}')">{{ s.code }}</td>
                        <td class="title">{{ s.title }}</td>
                        <td class="date hidden">{{ s.date }}</td>
                        <td class="all-dates">{{ ', '.join(s.all_dates) }}</td>
                        <td class="bible-references">{{ ', '.join(s.bible_references) }}</td>
                    </tr>
                    <tr data-index="{{ ns.index }}" class="details collapsed">
                        <td class="invisible"></td>
                        <td colspan="3">
                            <div class="detail-row"><span>Sammanhang:</span> {{ s.context }}</div>
                            <div class="detail-row"><span>Introduktion:</span> {{ s.introduction }}</div>
                            <div class="detail-row"><span>Budskap:</span> {{ s.message }}</div>
                            <div class="detail-row"><span>Kommentar:</span> {{ s.notes }}</div>
                            <div class="detail-row"><span>Omdöme:</span> {{ s.report }}</div>
                            <div class="detail-row"><span>Relaterad predikan:</span> {{ s.related_sermons }}</div>

                            <div class="detail-row">
                                <span>Manus:</span>{% if not s.manuscripts[0] %} –{% endif %}
                                {% for x in s.manuscripts %}
                                <br/>&nbsp;&nbsp;&nbsp;&nbsp;<span class="date">{{ x.date }}</span> <span class="file-name"><a href="../files/manuscripts/{{x.file_name}}">{{ x.file_name }}</a></span>  <span class="notes">{{ x.notes or '' }}</span>
                                {% endfor %}
                            </div>
                            <div class="detail-row">
                                <span>Resurs:</span>{% if not s.resources[0] %} –{% endif %}
                                {% for x in s.resources %}
                                <br/>&nbsp;&nbsp;&nbsp;&nbsp;<span class="title"><a href="../files/resources/{{ x.file_name }}">{{ x.title }}</a></span>  <span class="notes">{{ x.notes or '' }}</span>
                                {% endfor %}
                            </div>
                            <div class="detail-row">
                                <span>Gudstjänst:</span>{% if not s.services[0] %} –{% endif %}
                                {% for x in s.services %}
                                <br/>&nbsp;&nbsp;&nbsp;&nbsp;<span class="date">{{ x.date }}</span> <span class="place">{{ x.place }}</span>  <span class="notes">{{ x.notes or '' }}</span>
                                {% endfor %}
                            </div>
                            <div class="detail-row">
                                <span>Inspelning:</span>{% if not s.recordings[0] %} –{% endif %}
                                {% for x in s.recordings %}
                                    {% if x.file_name %}
                                        <br/>&nbsp;&nbsp;&nbsp;&nbsp;<span class="date">{{ x.date }}</span> <span class="file-name"><a href="../files/recordings/{{ x.file_name }}">{{ x.file_name }}</a></span>  <span class="type">({{ x.type }})</span> <span class="notes">{{ x.notes or '' }}</span>
                                    {% elif x.external_url %}
                                        <br/>&nbsp;&nbsp;&nbsp;&nbsp;<span class="date">{{ x.date }}</span> <span class="file-name"><a href="{{ x.external_url }}">{{ x.external_url }}</a></span>  <span class="type">({{ x.type }})</span> <span class="notes">{{ x.notes or '' }}</span>
                                    {% endif %}
                                {% endfor %}
                            </div>
                        </td>
                    </tr>
                    {% set ns.index = ns.index + 1 %}
                    {% endfor %}
                </tbody>
            </table>

        </main>
        <footer>
            <div>© Oskar Fornander 2026</div>
        </footer>
        <script type="text/javascript">
            {% include "sermon.js" %}
        </script>
    </body>
</html>
